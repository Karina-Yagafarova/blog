

    <% @articles.each do |article| %> <!--Пройтись по всем статьям-->
        <div>
          <div class="title" id="title2"> <%= article.title %></div>
          <div class="title"> <%= article.content %></div>
          <div><ul class="hr">
            <li> <%= article.created_at %> </li>
            <li><%= link_to 'Show', 'articles/' +article.id.to_s, method: :get%></li>
            <li><%= link_to 'Edit', 'articles/' +article.id.to_s+'/edit' %></li>
            <li><%= link_to 'Delete','articles/' +article.id.to_s , method: :delete, data: { confirm: 'Are you sure?' } %></li>
          </ul></div>
          <div class="line"><hr></div>
          <!-- <div class="title" id="title2">Scaffolding: Быстрее, легче, обольстительней</div>
          <div class="title">Они совершают свой удивительный пятнадцатиминутный подвиг, используя фичу под названием scaffolding, которая полагается в бОльшей степени на сгенерированный код, волшебно создаваемый Rails командой generate.</div> -->
        </div>
    <% end %>


<br>

<%= link_to 'New Article', new_article_path %>
